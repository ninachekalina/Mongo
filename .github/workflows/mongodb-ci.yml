name: MongoDB CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test-mongodb:
    runs-on: ubuntu-latest
    services:
      mongo:
        image: mongo:6
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --host localhost --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install MongoDB Shell (mongosh)
        run: |
          curl -fsSL https://www.mongodb.org/static/pgp/server-6.0.asc | sudo gpg --dearmor -o /usr/share/keyrings/mongodb-server-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/mongodb-server-keyring.gpg] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list
          sudo apt-get update
          sudo apt-get install -y mongodb-mongosh

      - name: Check MongoDB shell version
        run: mongosh --version

      - name: List running containers
        run: docker ps -a

      - name: Wait for MongoDB to be ready
        run: |
          echo "Waiting for MongoDB service..."
          sleep 10  # Уменьшил с 20 до 10 секунд
          for i in {1..15}; do  # Уменьшил количество проверок с 30 до 15
            if mongosh "mongodb://mongo:27017/admin" --eval "db.runCommand({ ping: 1 })" > /dev/null 2>&1; then
              echo "MongoDB is ready!"
              exit 0
            fi
            echo "Waiting for MongoDB to start ($i/15)..."
            sleep 2  # Уменьшил интервал с 3 до 2 секунд
          done
          echo "MongoDB did not start in time."
          exit 1


      - name: Run MongoDB script
        run: mongosh "mongodb://mongo:27017/testdb" --file test_queries.js

      - name: Show MongoDB logs on failure
        if: failure()
        run: docker logs $(docker ps -q --filter ancestor=mongo:6)
